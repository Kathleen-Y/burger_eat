{{! for the devoured. end result}}
<li>
	{{burger_type}}
	{{#if devoured}}
	<div id="devoured">
		<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">Devoured It!</button>
	</div>
	{{/if}}
</li>
